
<div class="event-card clearfix">
  <div class="event-card__image">
    {% include "atoms-calendar-icon" %}
  </div>
  <div class="event-card__detail">
  <h3><a href=#>{{ event.title }}</a></h3>
  </div>
  <div class="event-card__date-time">
    <p>Date: <time>{{ event.month.long }} {{ event.day.long }}, {{ event.year.long }}</time></p>
      {% if(event.hour) %}
    <p>Time: <time>{{ event.hour.short }}:{{ event.minute.long }}{{ event.hour.ampm }}</time></p>
      {% endif %}
  </div>
</div>
