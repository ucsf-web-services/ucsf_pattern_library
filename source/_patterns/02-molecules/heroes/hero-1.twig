<div class="slider slider-hero slider__hero-1">
    {% for slides in hero1_slides %}
        <div class="hero hero-1">
            <div class="hero-1__top-image">
                <div style="background-image:url('{{ img.banner1.src }}')">
                </div>
            </div>
            <div class="hero-1__text">
                    <h2>{{ hero.hero1.title }}</h2>
                    <p>{{ hero.hero1.paragraph }}</p>
                    <a href="#" class="btn btn--dark-transparent btn--small" id="Descriptive-Button-Action">Read more</a>
            </div>
        </div>
    {% endfor %}
</div>
